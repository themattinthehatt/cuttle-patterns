"""Frozen-pretrained-backbone embedders for `cuttle embed`.

See `cuttle_patterns.embed` for the extraction loop that drives these, and
`docs/implementation_notes/embedder.md` for the full design.
"""
